def mov(n, a, b, c):
    if(n == 1):
        print(a,'-->',c)
        //只有一个盘子，可以直接从a移动到c
    else:
        //通过分步减而治之
        mov(n-1, a, c, b)
        #先把上面的n-1个盘子借助c从a移到b
        mov(1, a, b, c)
       	//再把a最下面的一个盘子移到c
        mov(n-1, b, a, c)
        //再把b上的n-1个盘子借助a移动到c
mov(3,'A','B','C')