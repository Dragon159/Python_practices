# -*- coding: utf-8 -*-

def is_palindrome(n):

    strA = str(n)   #将n转化为字符串
    strB = strA[::-1]      #利用切片操作将字符串倒序
    if strA == strB:
        return True
# 测试:
output = filter(is_palindrome, range(10, 1000))
print(list(output))
